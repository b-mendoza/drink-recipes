import React from 'react';

const Header = () => {
    return (
        <header className="bg-alert">
            <h1>Search for Drink Recipes</h1>
        </header>
    );
};

export default Header;
